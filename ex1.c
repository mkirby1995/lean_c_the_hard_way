#include <stdio.h>

/* This is a comment. */

int main(int argc, char *argv[])
{
    int distance = 100;
    int b = 249;
    int m = 4;
    int x = 42;

    //this is also a comment
    printf("You are %d miles away.\n", distance);
    printf("M %d.\n", m * x + b);

    return 0;
}
